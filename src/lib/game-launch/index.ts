/** @deprecated Use `launch` — kept for call-site migration */
export { launch, launch as startGame } from "./launch";
export type { LaunchConfig, LaunchOptions } from "./resolve-config";
export { resolveLaunchConfig } from "./resolve-config";
